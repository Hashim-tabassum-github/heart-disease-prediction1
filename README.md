# heart-disease-prediction1
Project Description Heart Disease Prediction using Machine Learning A Flask-based web application that predicts the likelihood of heart disease based on user input. The model uses a pre-trained logistic regression model with data preprocessing, including scaling and label encoding.
